"""What happens when „1‟== 1 is executed?

 the expression "1" == 1 is executed in Python, it will return False.

This is because:

"1" is a string literal, representing the character "1".
1 is an integer literal, representing the numerical value 1. """
